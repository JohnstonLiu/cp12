#include <bits/stdc++.h>
using namespace std;

int main() {
    int M, Q;
    cin >> M >> Q;

    for(int i=0; i<Q; ++i) {
        //m-1 loop times and the remaining gets brain dead solved
        //remember the drawing you did
        //if this doesn't work try out the brute force method
        //if its divisible you already know M
    }
}